# Database
DATABASE_URL="postgresql://myuser:mypassword@localhost:5432/mydb?schema=public"

# MongoDB (pour chat)
MONGO_URI="mongodb://myuser:mypassword@localhost:27017/mydb"

# JWT Secrets
ACCESS_TOKEN_SECRET="your-super-secret-access-token-key-here-make-it-long-and-complex"
REFRESH_TOKEN_SECRET="your-super-secret-refresh-token-key-here-also-very-long-and-complex"

# Email (pour l'envoi de mails)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# Frontend URL (pour CORS et links)
FRONTEND_URL="http://localhost:5174"

# Environment
NODE_ENV="development"

# PostgreSQL (si utilisé séparément)
POSTGRES_HOST="localhost"
POSTGRES_PORT=5432
POSTGRES_USER="myuser"
POSTGRES_PASSWORD="mypassword"
POSTGRES_DB="mydb"

# MongoDB (si utilisé séparément)
MONGO_HOST="localhost"
MONGO_PORT=27017
MONGO_DB="mydb"
MONGO_USER="myuser"
MONGO_PASSWORD="mypassword"
